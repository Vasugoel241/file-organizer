## First Time Setup (Git install)
* git config --global user.email johndoe123@email.com
* git config --golbal user.name "John Doe"
* to verify git config --list


# (You need to do this always) when you create a new repository 
git init -> git repo create
create a file .gitignore => add node_module to init

git add.
git commit -m "commit message"



git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Vasugoel241/fs-organizer.git
git push -u origin main

…or push an existing repository from the command line
git remote add origin https://github.com/Vasugoel241/fs-organizer.git
git branch -M main
git push -u origin main